/** Host loader entry for the ProMentor dashboard browser implementation exported from `./client`. */

/** Host plugin body — the dashboard is a browser-only surface. */
export function apply(): void {}
